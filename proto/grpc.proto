syntax = "proto3";

package command;

import "gateway.proto";
import "online.proto";
import "pvp.proto";

service OnlinePvp {
    rpc OnlinePvpStream(stream Online2PvpInfo) returns (stream Pvp2OnlineInfo){};
}

service GatewayOnline{
    rpc GatewayOnlineStream(stream ClientPlayerMsgData) returns (stream  ServerPlayerMsgData){};
}

